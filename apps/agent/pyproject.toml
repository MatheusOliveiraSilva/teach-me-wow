[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph",
    "langchain_openai",
    "langchain_anthropic"
]

[tool.setuptools]
packages = ["api", "workflow"]
